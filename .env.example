# Stache Configuration
#
# Most settings have defaults in docker-compose.yml. This file is only needed
# for customization or when using OpenAI (which requires an API key).
#
# Quick Start (no .env needed):
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d

# ===== Embedding Model Selection (Ollama) =====
# Default is nomic-embed-text (768 dims). For higher quality:
# OLLAMA_EMBEDDING_MODEL=mxbai-embed-large
# EMBEDDING_DIMENSION=1024

# ===== OpenAI API (alternative to Ollama) =====
# Use docker-compose.openai.yml instead of docker-compose.local.yml
# OPENAI_API_KEY=sk-...

# ===== LLM Provider (optional - for AI synthesis) =====
# Default is none (search-only mode). To enable LLM:
# LLM_PROVIDER=ollama          # Use Ollama (free, local)
# LLM_PROVIDER=openai          # Use OpenAI (requires OPENAI_API_KEY)
# LLM_PROVIDER=anthropic       # Use Claude (requires ANTHROPIC_API_KEY)
# ANTHROPIC_API_KEY=sk-ant-...

# ===== Advanced Settings =====
# DATA_DIR=./data              # Where to store persistent data
# LOG_LEVEL=info               # debug, info, warning, error
# CHUNK_SIZE=2000              # Target chunk size
# CHUNK_OVERLAP=200            # Overlap between chunks
